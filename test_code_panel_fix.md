# Test Code Panel Fix

This file is to test that the code panel is always visible in the speedreader.

Here's some regular text before any code block.

```javascript
function hello() {
    console.log("Hello World!");
}
```

And some text after the code block.

```python
def greet(name):
    print(f"Hello, {name}!")
```

More text to continue reading.
