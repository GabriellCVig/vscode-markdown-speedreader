{
  "name": "markdownspeedreader",
  "displayName": "Markdown Speed Reader",
  "description": "A VSCode extension for speed reading markdown and text files with configurable reading speeds and controls.",
  "version": "1.0.1",
  "publisher": "gabriellvig",
  "license": "MIT",
  "engines": {
    "vscode": "^1.100.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GabriellCVig/vscode-markdown-speedreader.git"
  },
  "bugs": {
    "url": "https://github.com/GabriellCVig/vscode-markdown-speedreader/issues"
  },
  "homepage": "https://github.com/GabriellCVig/vscode-markdown-speedreader#readme",
  "categories": [
    "Other",
    "Education"
  ],
  "keywords": [
    "speed reading",
    "rsvp",
    "markdown",
    "reader",
    "mermaid"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "markdownspeedreader.speedreadFile",
        "title": "Speed Read Current File",
        "category": "Speed Reader"
      },
      {
        "command": "markdownspeedreader.speedreadSelection",
        "title": "Speed Read Selection",
        "category": "Speed Reader"
      },
      {
        "command": "markdownspeedreader.speedreadFromFile",
        "title": "Speed Read File...",
        "category": "Speed Reader"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "markdownspeedreader.speedreadFile",
          "when": "editorTextFocus",
          "group": "speedreader"
        },
        {
          "command": "markdownspeedreader.speedreadSelection",
          "when": "editorHasSelection",
          "group": "speedreader"
        }
      ],
      "explorer/context": [
        {
          "command": "markdownspeedreader.speedreadFromFile",
          "when": "resourceExtname == .md || resourceExtname == .txt || resourceExtname == .markdown",
          "group": "speedreader"
        }
      ]
    },
    "configuration": {
      "title": "Speed Reader",
      "properties": {
        "speedreader.defaultWPM": {
          "type": "number",
          "default": 250,
          "minimum": 50,
          "maximum": 1000,
          "description": "Default words per minute for speed reading"
        },
        "speedreader.pauseOnPunctuation": {
          "type": "boolean",
          "default": true,
          "description": "Pause briefly after punctuation marks"
        },
        "speedreader.fontSize": {
          "type": "number",
          "default": 24,
          "minimum": 12,
          "maximum": 48,
          "description": "Font size for the speed reading display"
        },
        "speedreader.inlineCodeMaxLength": {
          "type": "number",
          "default": 20,
          "minimum": 1,
          "description": "Maximum character length for inline code to stay inline in the reading flow. Longer inline code is shown in the code panel instead."
        },
        "speedreader.autoPauseOnDiagram": {
          "type": "boolean",
          "default": false,
          "description": "Automatically pause playback when a diagram (e.g. mermaid) block is reached, so it can be viewed. Resume manually with play."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "build:webview": "esbuild media-src/mermaid-entry.mjs --bundle --format=iife --minify --platform=browser --outfile=media/mermaid.bundle.js",
    "compile": "tsc -p ./ && npm run build:webview",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src",
    "test:unit": "mocha",
    "test:integration": "npm run compile && vscode-test",
    "test:browser": "npm run build:webview && playwright test",
    "test": "npm run lint && npm run test:unit && npm run test:integration"
  },
  "dependencies": {
    "markdown-it": "^14.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/jsdom": "^28.0.3",
    "@types/markdown-it": "^14.1.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/sinon": "^21.0.1",
    "@types/vscode": "^1.100.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.28.0",
    "eslint": "^9.25.1",
    "jsdom": "^29.1.1",
    "mermaid": "^11.15.0",
    "mocha": "^11.7.6",
    "sinon": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
